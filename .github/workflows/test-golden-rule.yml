name: test golden rule

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  golden-rule:
    name: golden test - rule (jdk ${{ matrix.java-version }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        java-version: [ '8', '17', '25' ]
    steps:
      - uses: actions/checkout@v6
      - name: set up java ${{ matrix.java-version }}
        uses: actions/setup-java@v5
        with:
          java-version: ${{ matrix.java-version }}
          distribution: 'temurin'
          cache: maven
      - name: build rule-test test project
        run: mvn -B -f test/rule-test/pom.xml -DskipTests package
      - name: prepare jar
        run: cp test/rule-test/target/rule-test-*.jar rule-test.jar
      - name: run golden rule test
        run: mvn -B test -Dtest=RuleGoldenTest -DfailIfNoTests=true
      - name: upload surefire reports
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: surefire-reports-golden-rule-jdk${{ matrix.java-version }}
          path: target/surefire-reports/
          if-no-files-found: ignore
      - name: clean
        if: always()
        run: rm -f rule-test.jar jar-analyzer.db jar-analyzer.db-wal jar-analyzer.db-shm
