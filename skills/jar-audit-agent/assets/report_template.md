# Java 安全审计报告

> 本报告由 **证据包（Evidence Pack）** 编译生成。所有结论必须能回溯到 `runs/<RUN_ID>/verify/*.jsonl` 中的证据引用。

## 0. 审计范围与环境

## 1. 总览（Coverage / 进度）

## 2. 关键发现（Top Risks）

## 3. 向量审计明细（按向量）

## 4. 未完成项与下一批计划（Fail-Closed Ledger）
